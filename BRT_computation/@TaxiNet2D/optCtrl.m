function uOpt = optCtrl(obj, ~, x, deriv, uMode, ~)
% uOpt = optCtrl(obj, t, y, deriv, uMode)

uOpt = [];

% %% Input processing
% if nargin < 5
%   uMode = 'max';
% end
% 
% %% Optimal control
% if strcmp(uMode, 'max')
%   uOpt{1} = obj.gainCTE * x{1} + obj.gainHE * x{2} * 180/pi;
% else
%   error('Unknown uMode!')
% end

end